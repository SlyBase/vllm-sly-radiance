# Claude Code

Follow [AGENTS.md](AGENTS.md): VERSION + CHANGELOG section for every image change, README options table for every knob, measurement rules (300 W, same-window A/B, second start, >= 128 runs for decode).
